# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# Environment type (development or production)
# In development: API docs enabled, CORS allows localhost
# In production: API docs disabled, CORS requires explicit ALLOWED_ORIGINS
ENVIRONMENT=development

# CORS - Allowed Origins (comma-separated)
# Only used in production. Leave empty or undefined for development.
# Example for production: https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=

# ============================================================================
# AI PROVIDER CONFIGURATION (for Threat Hunting features)
# ============================================================================
# Use Groq (free, fast) or OpenAI

# Groq API Key (recommended - free tier available)
# Get your key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# OpenAI API Key (fallback)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Default AI Provider (groq or openai)
AI_PROVIDER=groq

# Model Configuration
GROQ_MODEL=llama-3.1-70b-versatile
OPENAI_MODEL=gpt-4-turbo-preview

# ============================================================================
# SIGMA TRANSLATOR CONFIGURATION
# ============================================================================

# Path to Sigma rules repository
# Clone from: https://github.com/SigmaHQ/sigma
SIGMA_REPO_PATH=./sigma-rules

# Sigma Translator LLM Provider (offline, openai, azure, groq)
# offline = no LLM features (basic conversion only)
SIGMA_LLM_PROVIDER=offline

# Sigma LLM API Key (uses OPENAI_API_KEY or GROQ_API_KEY by default)
# Only needed if using a different key than above
SIGMA_LLM_API_KEY=

# Sigma LLM Model
SIGMA_LLM_MODEL=gpt-4o-mini

# Azure-specific (if using Azure OpenAI)
# SIGMA_LLM_API_BASE=https://your-resource.openai.azure.com/
# AZURE_API_VERSION=2024-02-15-preview
